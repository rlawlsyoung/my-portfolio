import React from "react";
import styled from "styled-components";

const TimeLine: React.FC = () => {
  return <StyledTimeLine>ㅎㅇㅋ</StyledTimeLine>;
};

const StyledTimeLine = styled.div``;

export default TimeLine;
