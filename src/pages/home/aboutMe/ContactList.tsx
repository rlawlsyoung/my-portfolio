import React from "react";
import styled from "styled-components";

const ContactList: React.FC = () => {
  return <StyledContactList></StyledContactList>;
};

const StyledContactList = styled.div``;

export default ContactList;
